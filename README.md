# RankedAPI

Small core module for mapping Elo ratings to ranks.
Used as a shared component across multiple projects.

This repository exists mainly for experimentation and inspiration.
Not intended as a standalone library.
